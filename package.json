{
  "name": "artblocks-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ ArtBlocks/artblocks-subgraph",
    "create-local": "graph create --node http://localhost:8020/ ArtBlocks/artblocks-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ ArtBlocks/artblocks-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ArtBlocks/artblocks-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
