version: 2.1

orbs:
  node: circleci/node@5.1.0

jobs:
  test:
    docker:
      - image: "cimg/base:stable"
    steps:
      - checkout
      - node/install:
          install-yarn: true
      - node/install-packages:
          pkg-manager: yarn
      - run: node --version
      - run:
          name: Run tests
          command: yarn test

workflows:
  run-ci:
    jobs:
      - test
